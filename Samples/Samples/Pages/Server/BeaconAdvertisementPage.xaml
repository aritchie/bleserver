<?xml version="1.0" encoding="utf-8" ?>
<acr:ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:acr="clr-namespace:Acr.XamForms;assembly=Acr.XamForms"
             x:Class="Samples.Pages.Server.BeaconAdvertisementPage">

    <ContentPage.Content>
        <TableView Intent="Form">
            <TableRoot>
                <TableSection>
                    <EntryCell Label="UUID" Text="{Binding Uuid}" />
                    <EntryCell Label="Major" Text="{Binding Major}" Keyboard="Numeric" />
                    <EntryCell Label="Minor" Text="{Binding Minor}" Keyboard="Numeric" />
                </TableSection>
                <TableSection>
                    <TextCell Text="{Binding AdvertiseText}" Command="{Binding AdvertiseToggle}" />
                </TableSection>
            </TableRoot>
        </TableView>
    </ContentPage.Content>
</acr:ContentPage>